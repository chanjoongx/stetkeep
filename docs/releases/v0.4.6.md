## v0.4.6 — README accuracy pass + Usage section (2026-04-21)

Pure documentation release. No functional or protocol changes.

### What changed

**Accuracy corrections**

- Token count corrected: "~1.8K each" → "~1–2K each (BRAIN ~1K · CRAFT ~1.5K · PERF ~1.8K)" — BRAIN.md was significantly overstated
- Anthropic docs URL updated to canonical `platform.claude.com` (docs.anthropic.com now 301-redirects)
- FP table example broadened: `` `constants.js` `` → "config / data file" to match the actual CRAFT.md false-positive definition
- Architecture diagram: "XML-catalog-driven" → "path, ignore, marker-driven" — the hook is path/marker-based, not XML-parsing
- Layer C description clarified in two places (Supporting layer + FAQ): `Write` (new file creation) is not in the toolset; `Edit` of existing files requires explicit user approval

**New: Usage section**

Added a Usage section between Install and Requirements with a 3-command reference table:

| Command | What it does |
|---|---|
| `/brain-scan [path]` | Map MD ecosystem, check Safety Net wiring, propose next steps |
| `/craft-audit [path]` | Read-only A1–A20 anti-pattern scan + FP cross-check, graded findings |
| `/perf-audit [scope]` | Measurement-first baseline, Top 5 bottlenecks by Impact × User-pain |

Both audit commands end with a Proceed prompt — nothing is edited without confirmation.

**Marketplace section split**

Install section now shows two distinct plugin paths:
- Self-hosted marketplace (`/plugin marketplace add chanjoongx/stetkeep`) — works today, GitHub-hosted catalog
- Anthropic official marketplace (`/plugin install stetkeep`) — submitted, under review

**Dynamic version badge**

Static `v0.4.5` badge replaced with a GitHub releases dynamic badge — auto-updates on `git tag` + `gh release create`, no manual bump needed.

**Polish**

- Requirements section emoji deduped (📦 → ⚙️)
- "restarting claude" → "restarting Claude Code"
- `CLAUDE.template.md` added to repo layout (was npm-distributed but unlisted)
- FAQ title "Why the brain metaphor?" → "Why BRAIN / CRAFT / PERF?"
- FAQ marketplace answer updated to reflect two plugin paths
- Benchmark pricing note removed (`~$18–25 per run`) — results pending, cost estimate was premature

### No functional changes

CLI behavior, hook decisions, plugin manifest, subagent tools, protocol structure: all identical to v0.4.5.
